# I2C Scanner

Classic Arduino I2C bus scanner — probes addresses 1..127 and prints any responding devices over serial. Useful for verifying sensor wiring (SDA→A4, SCL→A5 on Uno).

Sketch: `i2c_scanner.ino`.
