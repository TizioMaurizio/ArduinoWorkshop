# ServoFeedback

Reads an analog servo's internal feedback wire (A0) and a control potentiometer (A1), prints both over serial, and drives the servo on D6. Useful for plotting actual vs commanded angle.

Sketch: `ServoFeedback.ino`. Library: `Servo`.
