# AnimeColor

Python screen-capture color extractor (mss + OpenCV + Pillow). Computes the dominant/average color of the screen and streams it over serial to an Arduino driving a WS2812B strip — turns any monitor into an ambient backlight.

Entry: `AnimeColor.py`. Helpers: `Functions.py`, `NumberMapper.py`, `SerialTester.py`.
