# AmbientLeds

WS2812B (FastLED) strip on pin 7 with 240 LEDs, driven by serial RGB packets and two buttons (D12 color cycle, D13 intensity step). Includes smooth fade transitions between target and current color.

Sketch: `AmbientLeds.ino`. Library: FastLED.
