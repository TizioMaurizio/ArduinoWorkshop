# IrCodeGenerator

Receives IR signals on pin 7 and prints a ready-to-paste `case <n>: ... break;` snippet per remote button — pair it with `IrBlaster` to clone any IR remote.

Sketch: `IrCodeGenerator.ino`. Library: `IRremote`.
