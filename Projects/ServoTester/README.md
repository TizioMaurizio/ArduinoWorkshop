# ServoTester

Bench tool: cycles through the 16 channels of a PCA9685, exposing each one to a potentiometer (A0) or serial angle input. Stores per-channel home positions for safe startup.

Sketch: `ServoTester.ino`.
