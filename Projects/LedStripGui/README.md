# LedStripGui

Tkinter GUI with R/G/B sliders that sends color packets over serial (default COM7 @ 9600) to the `LedTie` Arduino sketch and reads the ack.

Entry: `SerialGui.py`. Companion sketch: `LedTie.ino`.
