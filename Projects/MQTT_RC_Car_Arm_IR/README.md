# MQTT_RC_Car_Arm_IR

Variant of `MQTT_RC_Car_Arm` with an additional IR transmitter — lets the car/arm trigger external IR devices (e.g. cloned remotes) on demand.

Sketch: `MQTT_RC_Car_Arm_IR.ino`. Libraries: `IRremote`, `Adafruit_PWMServoDriver`.
