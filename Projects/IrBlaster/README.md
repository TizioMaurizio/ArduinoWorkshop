# IrBlaster

IR transmitter that reads an integer from serial and emits the matching NEC hex code on pin 3. Codes are organised in a switch — generate new entries with `IrCodeGenerator`.

Sketch: `IrBlaster.ino`. Library: `IRremote`.
