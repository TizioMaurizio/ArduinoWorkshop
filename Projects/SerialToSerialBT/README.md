# SerialToSerialBT

ESP32 bridge that turns a smartphone's Bluetooth Serial app into an IR remote — relays BT serial bytes to an IR transmitter on pin 15 and blinks D13 on activity.

Sketch: `SerialToSerialBT.ino`. Libraries: `BluetoothSerial`, `IRremote`.
