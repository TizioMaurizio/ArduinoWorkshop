# 4digit — 7-Segment Clock

Drives a common-anode 4-digit 7-segment display on Arduino Uno as a counting clock.
Pins `a..g` and `dp` on D2-D8/D13, digit selects `d1..d4` on D9-D12.
Adjust `del` to fine-tune timing.

Sketch: `4digit.ino`.
