# LedTie

Wearable WS2812B strip controller (240 LEDs, FastLED). Two buttons (D12 = color, D13 = intensity) cycle through colors and brightness for performance/dance use; also accepts serial RGB packets.

Sketch: `LedTie.ino`.
