# Arduino Servo Simulation

Tkinter GUI that sends servo angle commands (0–180) over serial (default COM4 @ 115200) to an Arduino Uno. Wiring: D9 → servo signal, 5V/GND. Includes a C++ source set in case you need to compile from scratch.

Entry: `gui.py` and `main.cpp`.
