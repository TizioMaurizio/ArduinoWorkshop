# MPU6050_Mouse

MPU6050 accelerometer + gyroscope reader on Uno. Computes roll/pitch/yaw from raw samples and streams them over serial — designed to act as a head/hand tracker that a host PC turns into mouse input.

Sketch: `MPU6050_Mouse.ino`. Library: `Wire`.
