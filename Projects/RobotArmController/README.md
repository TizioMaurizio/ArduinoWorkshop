# RobotArmController

Android-friendly Tkinter/Kivy arm controller using `usb4a` + `usbserial4a` to stream servo angles to the Arduino over USB OTG.

Entry: `ArduinoTkinterArmController.py`. Sketch: `RobotArmController.ino`.
