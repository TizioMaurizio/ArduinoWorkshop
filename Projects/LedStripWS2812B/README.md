# LedStripWS2812B

Minimal WS2812B (FastLED) driver on pin 7 / 240 LEDs. Reads `RRRGGGBBB` formatted serial strings and applies the color across the strip.

Sketch: `LedStripWS2812B.ino`. Library: FastLED.
